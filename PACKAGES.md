# Packages

| Registry | Package |
|----------|---------|
| **npm** (GitHub Packages) | `@beig-rules/security-audit-pro` |
| Containers / Maven / NuGet / RubyGems | Not applicable (documentation skill package) |

```bash
echo "@beig-rules:registry=https://npm.pkg.github.com" >> ~/.npmrc
npm install @beig-rules/security-audit-pro
```
